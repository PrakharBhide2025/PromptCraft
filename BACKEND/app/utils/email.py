# app/utils/email.py

def send_email(subject: str, recipient: str, body: str):
    # Replace with actual email sending logic (e.g., SMTP or SendGrid)
    print(f"Sending email to {recipient}: {subject}\n{body}")
